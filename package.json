{
  "name": "world-tester",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered QA tester — monorepo with agent service and web frontend",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "agent": "npm run start -w @world-tester/agent",
    "agent:server": "npm run server -w @world-tester/agent",
    "agent:dev": "npm run dev -w @world-tester/agent",
    "web": "npm run dev -w @world-tester/web",
    "web:build": "npm run build -w @world-tester/web",
    "dev": "bash scripts/dev.sh",
    "release": "AGENT_MODE=server VNC=true docker compose down && AGENT_MODE=server VNC=true docker compose up --build -d",
    "release:interactive": "docker compose run --rm --build agent",
    "release:interactive:vnc": "(while ! nc -z localhost 5900 2>/dev/null; do sleep 1; done && npm run vnc) & VNC=true docker compose run --rm --build --service-ports agent",
    "release:agent-only": "docker compose up --build -d agent",
    "release:logs": "docker compose logs -f",
    "release:down": "docker compose down",
    "vnc": "command -v vncviewer >/dev/null 2>&1 && vncviewer localhost:5900 || echo 'vncviewer not found. Install it: sudo apt install tigervnc-viewer'",
    "db:generate": "npm run db:generate -w @world-tester/agent",
    "db:push": "npm run db:push -w @world-tester/agent",
    "postinstall": "npm run db:generate -w @world-tester/agent 2>/dev/null; npx -w @world-tester/agent playwright install chromium 2>/dev/null; true"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
