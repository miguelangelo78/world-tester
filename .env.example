# === World Tester Configuration ===

# Google Gemini API key (required)
GOOGLE_GENERATIVE_AI_API_KEY=your_google_api_key_here

# PostgreSQL connection (required)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/worldtester

# Browser settings
HEADLESS=false

# Target website URL (optional, can also be set per command)
TARGET_URL=

# Agent WebSocket server port (default 3100)
AGENT_PORT=3100

# Frontend WebSocket URLs (set when agent runs on a different host)
# NEXT_PUBLIC_AGENT_WS_URL=ws://localhost:3100
# NEXT_PUBLIC_VNC_WS_URL=ws://localhost:5901

# Enable VNC for visual debugging in Docker
# VNC=true
