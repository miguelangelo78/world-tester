# === World Tester Configuration ===

# Google Gemini API key (required)
GOOGLE_GENERATIVE_AI_API_KEY=your_google_api_key_here

# PostgreSQL connection (required)
# Use docker compose up db for a local Postgres, or set your own
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/worldtester

# Browser settings
HEADLESS=false

# Target website URL (optional, can also be set per command)
TARGET_URL=
